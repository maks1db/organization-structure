export interface ActionType {
  id: string;
  anchor: HTMLElement;
}

export interface ActionEmployeeType {
  x: number;
  y: number;
  anchor: HTMLElement;
}

export interface ExcludeIds {
  excludeIds: string[];
}
